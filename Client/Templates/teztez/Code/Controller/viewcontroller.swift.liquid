//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

protocol {{ module_info.name }}Presentable: Presentable {}

final class {{ module_info.name }}ViewController: ViewController, {{ module_info.name }}Presentable {
    private let store: {{ module_info.name }}Store
    
    init(store: {{ module_info.name }}Store) {
        self.store = store
        super.init(nibName: String(describing: Self.self), bundle: nil)
    }
    
    required init?(coder: NSCoder) {
        return nil
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        setupObservers()
    }

    private func setupObservers() {
        store.$state.sink { [weak self] state in
            guard let self = self else { return}
        }
    }
}
